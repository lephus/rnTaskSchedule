export interface TaskItemOfCategory {
  id: number;
  taskName: string;
  checked: boolean;
}

export interface TaskItem {
  createAt: any;
  date: any;
  description: string;
  id: string;
  status: string;
  title: string;
}
